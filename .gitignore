# generated by tooling; never committed or frozen
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
dist/
build/

# secrets: API keys live here in Stage 1; must never enter git
.env

# macOS noise
.DS_Store

# transient draw output (the four freeze artifacts in out/ ARE committed)
out/pending_s5.csv

# derived deposit archive (redundant with the commit it describes)
out/deposit_bundle.tar.gz