# generated by tooling; never committed or frozen
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
dist/
build/

# secrets: API keys live here in Stage 1; must never enter git
.env

# macOS noise
.DS_Store

# transient draw output (the four freeze artifacts in out/ ARE committed)
out/pending_s5.csv

git add .gitignore pyproject.toml docs/ reference/ scripts/make_study_keys.py scripts/build_treated_forks.py src/trust_signals/treatments/ tests/test_treatments.py
git diff --cached --name-only | grep -E "^keys/|\.pem$|gnupg|private" ; echo "exit $?"